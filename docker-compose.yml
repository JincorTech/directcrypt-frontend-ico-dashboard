version: '2'
services:
  frontend-ico-dashboard:
    image: jincort/directcrypt-frontend-ico-dashboard:production
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
       - "80:80"
    tty: true